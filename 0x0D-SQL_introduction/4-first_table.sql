-- Creates the table first_table in the current database in my MySQL server,
-- name of the database passed as argument of mysql command.
-- This SQL query creates first_table with the id and name fields.
CREATE TABLE IF NOT EXISTS first_table(
	id int,
	name VARCHAR(256)
	)
