-- Inserts a new row in the table first_table (database hbtn_0c_0)
-- in my MySQL server.
-- The database name is passed as an argument of the mysql command.
-- This SQL query inserts a new row in the table.
INSERT INTO first_table (id, name) VALUES(
	89, 'Best School')
