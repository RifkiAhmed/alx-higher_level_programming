-- Computes the score average of all records in the table second_table.
-- The database name is passed as an argument of the mysql command.
-- This SQL query prints average of scores in the table as 'average column'
SELECT avg(score) as average FROM second_table;
